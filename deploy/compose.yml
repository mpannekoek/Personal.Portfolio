services:
  app:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_NAME:-mpannekoek/personal.portfolio}:${IMAGE_TAG:-latest}
    container_name: personal-portfolio
    restart: unless-stopped
    ports:
      - "3000:3000"
